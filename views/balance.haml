%div.row
  %div.col-md-6.col-md-offset-3
    %table.well.table{style: "font-size:200%;"}
      %thead
        %tr
          %td
          %td Expenses
          %td ±
      %tbody
        - User.each do |user|
          %tr
            %td= user.name
            %td
              %strong= @stats[user.id][:expenses]
            %td
              %strong{class: @stats[user.id][:difference] >= 0 ? "income" : "expense" }= @stats[user.id][:difference]

%div.row
  %div.col-md-2.col-md-offset-5
    %a.btn.btn-success{href: "/balance/all"} All shared transactions
%div.row{style: "margin-top:40px;"}
  %div.col-md-8.col-md-offset-2
    - @month_names_count.each_with_index do |e,i|
      %div.col-md-4{style: "margin-top:20px"}
        %a.btn.btn-info{href: "#{e[:url]}", style: "width:100%"}
          %span.pull-left= "#{e[:name]}"
          %span.badge.pull-right= "#{e[:count]}"
